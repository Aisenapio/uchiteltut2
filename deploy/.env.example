# Docker Compose Environment Variables
# Copy this file to .env and adjust values

# JWT Secret for authentication tokens
JWT_SECRET=your_super_secret_jwt_key_change_in_production

# Tuna tunnel token (optional - for public URL)
# TUNA_TOKEN=your_tuna_token_here

# MongoDB credentials (already set in docker-compose)
# MONGODB_URI=mongodb://admin:admin123@mongodb_teachers:27017/teachers?authSource=admin

# Server port (already set in docker-compose)
# PORT=4000

# Tuna subdomain (already set in docker-compose)
# TUNA_SUBDOMAIN=teachers

# Client environment variables (already set in docker-compose)
# VITE_API_URL=/graphql
# VITE_UPLOADS_URL=/uploads